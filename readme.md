# Fetching API with React Saga, RTK and React Query

Для запуска проекта необходимо перейти в папку `server`
и выполнить `yarn start`

После запустить нужный клиент `rtk-test` или `saga-test` или `react-query-test`
